import SectionHeader from "../../components/SectionHeader";

export default function ContactHeader() {
  return (
    <SectionHeader
    title="Contact"
    subtitle="Get in Touch With Us"
    description="Have a question or want to start a project? Let’s connect.Fill out the form or reach us directly using the details below."
    />
  );
}
